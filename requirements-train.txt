# Training-time dependencies for the neural-network pipeline.
# NOT installed in the deployed backend/Vercel function: runtime inference is
# pure NumPy (see ml/nn_forward.py). Install locally with:
#     python -m venv .venv-train && .venv-train\Scripts\pip install -r requirements-train.txt
torch==2.13.0+cpu
numpy
pandas==3.0.*
pytest==8.*